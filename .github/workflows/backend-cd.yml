name: Backend CD (Render Deployment)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    # Requires the 'test' job from the Backend CI workflow to pass first
    needs: test
    runs-on: windows-latest
    steps:
      - name: Deployment Triggered
        run: echo "Deployment for the backend (Web Service) is automatically handled by Render upon push to main."
      
      - name: Document Deployment Location
        run: echo "Check Render Dashboard Events tab for web service deployment status.